lottie.loadAnimation({
  container: document.getElementById('ball-1'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: '../assets/svgs-animated/ball-1/ball.json' // the path to the animation json
});

lottie.loadAnimation({
  container: document.getElementById('ball-2'), // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: '../assets/svgs-animated/ball-2/ball.json' // the path to the animation json
});

